# 8051-Event-Counter-using-Timer
A simple 8051 microcontroller project written in Assembly language that counts input pulses and displays the result on a 3-digit multiplexed 7-segment display.  The system detects pulses from an input pin and increments a counter value. The count is then split into digits and shown on the display using multiplexing.
